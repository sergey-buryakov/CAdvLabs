#include <iostream>
#include <map>
#include <string>

using namespace std;

//template<class T> void print(T start, T end) {
//        while (start != end) {
//                cout << start->first << " result is : " << start->second << endl;
//                start++;
//        }
//    }

int main()
{
	map<string, int> results = { {"Sarah", 15 }, {"John", 12 }, {"Bart", 17} };
	//print(results.begin(), results.end());
	for(auto result : results)
        cout << result.first << " result is : " << result.second << endl;
	cout << endl;

	results.erase("Bart");
	//print(results.begin(), results.end());
	for(auto result : result)
        cout << start->first << " result is : " << start->second << endl;
	cout << endl;

	results.insert(pair<string,int>("Bob",16));
	//print(results.begin(), results.end());
	for(auto result : result)
        cout << start->first << " result is : " << start->second << endl;

	return 0;
}
